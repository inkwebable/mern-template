export enum APILogin {
  Index = '/api/login',
}

export enum APILogout {
  Index = '/api/logout',
}

export enum APISignUp {
  Index = '/api/signup',
}

export enum APIUser {
  Index = '/api/user',
}

const ApiRoutes = {
  APILogin,
  APILogout,
  APISignUp,
  APIUser,
};

export default ApiRoutes;
