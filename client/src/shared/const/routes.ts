export enum Home {
  Index = '/',
}

export enum Profile {
  Index = '/profile',
}

export enum Login {
  Index = '/login',
}

export enum SignUp {
  Index = '/signup',
}


const AppRoutes = {
  Home,
  Profile,
  Login,
  SignUp,
};

export default AppRoutes;
