import styled from 'styled-components';

import { fontSizes } from '../../assets/styles/settings';

export const PageTitle1 = styled.h1`
  font-size: ${fontSizes.xxl};
`;

export const PageTitle2 = styled.h2`
  font-size: ${fontSizes.xl};
`;
